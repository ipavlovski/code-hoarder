import Header from 'components/header'

export const metadata = {
    title: 'Test nov1 first..',
    category: ['cat2']
}

<Header title={metadata.title} category={metadata.category} />


# {metadata.title} - {metadata.category}

# This is another example MDX page (nov11, first post)
 
Token components...

Again, **bold** and _italics_.

import MyComponent2 from './component-2'

<MyComponent2 />

A code box:
```bash
echo test 123
```

This is a list:
- one
- two
- three

And another react component:


import MyComponent from './component-1'

<MyComponent />
